@using BelibaHoma.BLL.Enums
@using BelibaHoma.BLL.Models
@using Extensions.Enums
@model IEnumerable<BelibaHoma.BLL.Models.GradeModel>
@{
    var user = ViewBag.CurrentUser as UserModel;
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SemesterNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UpdateDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SemesterType)
        </th>
        <th>
            
        </th>
    </tr>

        @foreach (var item in Model)
        {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SemesterNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.UpdateDate)
        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.SemesterType)*@
            @item.SemesterType.ToDescription()
        </td>
        <td>
            @*@Html.ActionLink("עריכה", "Edit", new { id = item.TraineeId, semesterNumber = item.SemesterNumber/* id=item.PrimaryKey */ })*@
            <a class="btn btn-default btn-xs" href="/Trainee/Grade/Edit/?id=@item.TraineeId&semesterNumber=@item.SemesterNumber">עריכה</a>
            @{
                if (user != null && (user.UserRole == UserRole.Admin || user.UserRole == UserRole.Rackaz))
                {
                    @*@Html.ActionLink("מחיקה", "Delete", new { id = item.TraineeId, semesterNumber = item.SemesterNumber /* id=item.PrimaryKey */})*@
                    @Html.Raw(" |")
                    <a class="btn btn-default btn-xs" href="/Trainee/Grade/Delete/?id=@item.TraineeId&semesterNumber=@item.SemesterNumber">מחיקה</a>
                }
            }
        </td>
    </tr>
}

</table>
